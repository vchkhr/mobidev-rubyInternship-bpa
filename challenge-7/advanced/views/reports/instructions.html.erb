<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <title>Installation Instractions</title>
  </head>

  <body style="padding: 1em">
    <div class="d-flex mt-3" style="height: 100px;">
      <div class="d-flex" style="width: 200px; border: 1px solid black; align-items: center; justify-content: center;">LOGO</div>
      <div class="flex-fill h-100">
        <div class="d-flex h-100" style=" align-items: center; justify-content: center;">
          <div>
            <h1 class='text-center'>
              <% unless @office.class == Hash %>
                Installation Instractions
              <% else %>
                <%= @office['name'] %>
              <% end %>
            </h1>

            <% if @office.class == Hash %>
              <h4 class='text-center'>
                <%= @office['state'] %>, <%= @office['address'] %>, <%= @office['phone'] %>, <%= @office['type'] %><br />
                <%= @office['area'] %><br />
                <%= @office['max_people'] %>
              </h4>
            <% end %>
          </div>
        </div>
      </div>
    </div>

    <div>
      <%= @instructions %>

      <% if @instructions.empty? %>
        <div class="alert alert-danger mt-3 mb-3 m-auto" role="alert" style="width: 500px;">
          There is no data for the office with this ID: <strong><%= @office %></strong>.
        </div>
      <% end %>
    </div>

    <p class="text-center">
      <a href="/">Go Home</a>
      <% if @instructions.empty? || @office.class == Hash %>
        | <a href='/instructions/offices'>All offices</a>
      <% end %>
    </p>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
  </body>
</html>
